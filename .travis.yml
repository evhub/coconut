language: python

sudo: false

notifications:
  email: false

matrix:
  include:

    - python: "2.6"

      install:
        - pip install .
        - pip install autopep8
        - pip install futures

      script:
        - python -m unittest tests

    - python: "2.7"

      install:
        - pip install .
        - pip install autopep8
        - pip install futures

      script:
        - python -m unittest tests

    - python: "pypy"

      install:
        - pip install .
        - pip install autopep8
        - pip install futures

      script:
        - python -m unittest tests

    - python: "3.2"

      install:
        - pip install .
        - pip install autopep8

      script:
        - python -m unittest tests

    - python: "3.3"

      install:
        - pip install .
        - pip install autopep8

      script:
        - python -m unittest tests

    - python: "3.4"

      install:
        - pip install .
        - pip install autopep8

      script:
        - python -m unittest tests

    - python: "3.5"

      install:
        - pip install .
        - pip install autopep8

      script:
        - python -m unittest tests

    - python: "pypy3"

      install:
        - pip install .
        - pip install autopep8

      script:
        - python -m unittest tests
